/*
 * LoginAlertDialog Messages
 *
 * This contains all the text for the LoginAlertDialog component.
 */

import { defineMessages } from 'react-intl';

export default defineMessages({
  header: {
    id: 'app.components.LoginAlertDialog.header',
    defaultMessage: 'This is the LoginAlertDialog component !',
  },
});
