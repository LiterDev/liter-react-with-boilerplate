/*
 * ProfileIdentity Messages
 *
 * This contains all the text for the ProfileIdentity component.
 */

import { defineMessages } from 'react-intl';

export default defineMessages({
  header: {
    id: 'app.components.ProfileIdentity.header',
    defaultMessage: 'This is the ProfileIdentity component !',
  },
});
