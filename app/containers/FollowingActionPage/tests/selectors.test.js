// import { fromJS } from 'immutable';
// import { selectFollowingActionPageDomain } from '../selectors';

describe('selectFollowingActionPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
