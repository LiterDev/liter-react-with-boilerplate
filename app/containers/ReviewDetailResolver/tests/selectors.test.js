// import { fromJS } from 'immutable';
// import { selectReviewDetailResolverDomain } from '../selectors';

describe('selectReviewDetailResolverDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
